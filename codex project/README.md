# Sampurna Pal Portfolio Village

A lightweight React + Tailwind portfolio built as an interactive strategy-village map.

## Run Locally

```bash
npm install
npm run dev
```

Then open the local URL Vite prints in the terminal.

## Build

```bash
npm run build
```

The project uses original CSS and SVG artwork only. No copyrighted game assets are included.
